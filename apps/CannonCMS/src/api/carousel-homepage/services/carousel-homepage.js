'use strict';

/**
 * carousel-homepage service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::carousel-homepage.carousel-homepage');
