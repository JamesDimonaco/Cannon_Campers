'use strict';

/**
 * conversation-package controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::conversation-package.conversation-package');
