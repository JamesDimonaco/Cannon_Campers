'use strict';

/**
 * conversation-package router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::conversation-package.conversation-package');
